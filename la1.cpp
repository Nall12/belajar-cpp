#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    while (n > 0) {
        cout << n;
    }
    if (n>=) {
    cout << "+";
    }
    else
    cout << jumlah += digit;
}